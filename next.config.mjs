/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com", // Regular Unsplash images
      "plus.unsplash.com", // Premium Unsplash images
    ],
  },
};

export default nextConfig;
